# Webtop setup script

This is a opinionated repo init script for web docker arch container environment from linuxserver image. Currently using image arch-kde.


installed apps need to run with --no-sandbox and gpg key handler needs to be installed, kde already comes with one.


## Installation
```bash
sh ./install.sh
```



